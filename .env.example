# LDAP / Active Directory Configuration
LDAP_URL=ldap://10.27.16.5
LDAP_BASE_DN=DC=swd,DC=local
LDAP_DEFAULT_UPN=swd.bh
LDAP_ALT_UPN=swd.local
LDAP_NETBIOS=SWD

# LDAP Service Account (for directory search - optional, uses current user if not set)
LDAP_SERVICE_USER=
LDAP_SERVICE_PASS=

# Session Configuration
SESSION_SECRET=change_this_long_random_string

# CORS Configuration (comma-separated if multiple origins)
CORS_ORIGINS=http://localhost:3000

# Server Configuration
PORT=3000
APP_URL=http://localhost:3000

# Email Configuration (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@example.com
SMTP_PASS=your_smtp_password
SMTP_FROM=Job Order System <noreply@example.com>

# Signature Settings
SIGNATURE_AUTO_APPROVE=false

# Admin Emails (comma-separated) - only these users can manage signatures
ADMIN_EMAILS=a.kandi@swd.bh,admin-kandi@swd.bh,m.adil@swd.bh,admin-adil@swd.bh,h.hayat@swd.bh,admin-hayat@swd.bh,a.khairi@swd.bh,admin-khairi@swd.bh,support@swd.bh,a.alkubaesy@swd.bh
